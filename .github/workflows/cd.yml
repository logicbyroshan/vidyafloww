name: CD — Deploy

# Disabled automatic push triggers — deployment is only triggered manually via workflow_dispatch
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "staging"
        type: choice
        options: [staging, production]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ─── Deploy Staging ─────────────────────────────────────────────────────────
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      # TODO: Add Docker build, push, and deployment steps
      # Steps will vary based on hosting provider (AWS ECS, GCP Cloud Run, etc.)
      - name: Placeholder — Deploy Staging
        run: echo "TODO — Implement staging deployment pipeline"

  # ─── Deploy Production ──────────────────────────────────────────────────────
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production
    needs: deploy-staging
    if: github.event.inputs.environment == 'production'
    steps:
      - uses: actions/checkout@v4

      # TODO: Add production deployment steps
      - name: Placeholder — Deploy Production
        run: echo "TODO — Implement production deployment pipeline"
